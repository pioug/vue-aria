# Spectrum Style Readiness Tracker

This file tracks style readiness for Spectrum package slices.

- `ready`: docs/base styling is close enough to upstream for manual parity validation.
- `pending`: style parity is still visibly behind upstream.

Completed packages in `SPECTRUM_PORTING_TRACKER.md` must be `ready` here.

| package | status | notes |
| --- | --- | --- |
| `icon` | ready | Baseline icon wrappers and sizing classes are mapped in docs styles. |
| `form` | ready | Form layout and field group styling are present in docs baseline. |
| `label` | ready | Label/helptext styles are present and usable in docs previews. |
| `layout` | ready | Grid/Flex visual spacing baseline is covered for previews. |
| `text` | ready | Heading/text/keyboard baseline typography classes are covered. |
| `view` | ready | Header/content/footer view containers have baseline styles. |
| `buttongroup` | ready | Button group and segmented cluster styles are present. |
| `link` | ready | Link visual states are mapped in baseline docs styles. |
| `accordion` | ready | Accordion container, trigger, and panel styles are covered. |
| `switch` | ready | Switch track/thumb/label baseline styling is present. |
| `filetrigger` | ready | FileTrigger is behavior-first and relies on styled trigger controls. |
| `illustratedmessage` | ready | Message container/heading/content baseline is present. |
| `avatar` | ready | Avatar size/shape baseline is present in docs style layer. |
| `badge` | ready | Badge variants and pill styling baseline is present. |
| `divider` | ready | Horizontal and vertical divider baselines are present. |
| `image` | ready | Image frame and sizing baseline is present. |
| `labeledvalue` | ready | Label-value block and text styling baseline is present. |
| `meter` | ready | Meter track/fill/value baseline styling is present. |
| `progress` | ready | Progress bar/circle baseline visual styles are present. |
| `statuslight` | ready | Status light dot/text variant colors are present. |
| `well` | ready | Well container baseline style is present. |
| `inlinealert` | pending | Needs closer upstream-like border/layout/icon style parity validation. |
